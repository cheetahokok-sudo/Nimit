# Nimit — Development Roadmap

> สถานะ ณ 26 ก.ค. 2569 (2026-07-26). สามแทร็กเดินคู่กัน: **App** (Flutter),
> **Library** (คลังตำรา), **Platform** (โครงสร้างพื้นฐาน).
> เอกสารกลยุทธ์ธุรกิจอยู่ใน repo ส่วนตัว `Nimit-docs` — ไฟล์นี้คือแผนพัฒนาเท่านั้น

**Live:** app https://cheetahokok-sudo.github.io/Nimit/ · backend Supabase
`ap-southeast-1` · ทุก commit ผ่านสอง CI gate (Flutter analyze+test, DB
migrations+assertion suite)

---

## Track A — App (Flutter)

| Phase | ขอบเขต | เกณฑ์ว่าเสร็จ (definition of done) | สถานะ |
|---|---|---|---|
| A0 Scaffold | 8 หน้าจอตาม UI board v2, 5 แท็บ, ภาษาไทยเป็นหลัก, mock data, journal/เลข/งบ เก็บในเครื่อง | ทุกหน้าจอเดินได้จริง, `flutter test` เขียว, deploy ขึ้น Pages | ✅ |
| A1 Live read | อ่านข้อมูลจริงชุดแรกจาก backend | หน้าแหล่งอ้างอิงแสดงจำนวนจริงจากฐานข้อมูล (46) ผ่าน `PostgrestSourcesRepository` | ✅ |
| A2 Dream analysis จริง | `analyze()` เรียก Edge Function ที่สแกนข้อความฝันแบบ longest-match กับ `symbol_term` แล้วคืนคำแปลจริงพร้อม citation | ฝันเรื่องงูในแอป → ได้คำแปลจากมหาสุบินชาดกพร้อมป้าย A2 และที่มา; mock ยังเป็นค่า default ของเทสต์ | ⬜ ถัดไป |
| A3 Library browser | ปุ่ม "เปิดคลังตำรา" ใช้งานจริง: ค้นสัญลักษณ์, หน้ารายละเอียดพร้อมคำแปล+บรรณานุกรม+quote ที่ชอบด้วยกฎหมาย | ผู้ใช้ตามรอยจากคำแปลไปถึงระเบียนต้นฉบับได้ในสองแตะ | ⬜ |
| A4 Auth + ข้อมูลผู้ใช้ | Anonymous sign-in, journal ย้ายขึ้น cloud (สมัครใจ), `public` schema + RLS per-uid, consent log แบบ append-only, ลบ/ส่งออกข้อมูลได้ | ผ่านรีวิว PDPA โดยที่ปรึกษากฎหมายก่อนเปิด; privacy notice ภาษาไทยเขียนก่อน schema | ⬜ |
| A5 Trends จริง | กระแสจากข้อมูลจริงภายใต้ k-anonymity (≥25 ผู้ใช้ ≥3 จังหวัดต่อหนึ่งเทรนด์) | หน้ากระแสแสดงข้อมูลจริงโดยพิสูจน์ได้ว่าไม่เปิดเผยรายบุคคล | ⬜ |

**นโยบายถาวรของ A2:** ตัวจับคู่เป็น deterministic เท่านั้น หาก AI เข้ามาช่วยภายหลัง
ทำได้แค่ *จัดอันดับ* คำแปลที่ค้นเจอ ห้ามแต่งข้อความคำแปลเอง — ข้อความที่แต่งขึ้น
ไม่มี citation และติดป้าย tier อย่างซื่อสัตย์ไม่ได้

## Track B — Library (คลังตำรา)

| Phase | ขอบเขต | เกณฑ์ว่าเสร็จ | สถานะ |
|---|---|---|---|
| B0 ทะเบียนแหล่ง | 40 works / 48 editions พร้อม trust tier A1–D และสถานะสิทธิ์สองแกน (ตัวบท vs สำเนา) | บรรณานุกรม published, `api.cite` ใช้งานได้ | ✅ |
| B1 ศัพท์ค้นหา | 59 สัญลักษณ์ + 134 คำค้น (คำหลัก/พ้อง/ถิ่น/สะกดผิด) | ค้น "งูเหลือม" เจอถูกตัว, ranking ตามน้ำหนัก | ✅ |
| B2 เนื้อหาชุดแรก | มหาสุบินชาดก: 1 passage (คาถา ฉบับหลวง อ้าง verbatim ได้เพราะ PD) + 5 คำแปลผูกสัญลักษณ์ พร้อมแหล่งประกอบและ review record | `get_symbol('snake')` คืนคำแปลจริงพร้อม quote; **ศูนย์เลขเด็ดจากพุทธวรรณกรรม — ตลอดไป** | ✅ วันนี้ |
| B3 ขอสิทธิ์สถาบัน | ส่งหนังสือถึง หอสมุดแห่งชาติ (ทะเบียน 447, 369, พรหมชาติ), ศมส. (NPT003-010, RBR003-012), กรมศิลปากร; ยื่นขอ digitise Wellcome MS Thai 10 | ได้หนังสือตอบเป็นลายลักษณ์อักษร บันทึกใน `editorial.rights_grant` | ⬜ **รอผู้ใช้ส่ง** (ร่างพร้อมใน Nimit-docs) |
| B4 ตรวจ PD เพิ่ม | ยืนยันปีพิมพ์กรมศิลปากร 2505/2508 จากหน้าลิขสิทธิ์จริง; ปีถึงแก่กรรมหลวงวิศาลดรุณกร (ปลดล็อก Wikisource ฤกษ์/นพเคราะห์); สืบต้นทางวิกิซอร์ซ "คำทำนายฝัน" | สถานะสิทธิ์เปลี่ยนจาก unknown เป็นข้อสรุปที่มีหลักฐาน | 🔄 กำลังสืบ |
| B5 Transcription pilot | ปริวรรตจากภาพต้นฉบับ 4 กลุ่ม (จักรทีปนี, ทำนายฝัน, พรหมชาติ, ตำราฤกษ์) แบบ double-key เมื่อได้สิทธิ์/สำเนา | mini-corpus 4 ชุดที่ผู้อ่านสองคนตรวจตรงกัน | ⬜ |
| B6 Scale editorial | ผู้ร่วมเขียนหลายคน, คิวรีวิว, canary entries | Studio พอสำหรับบรรณาธิการเดี่ยว; เกินนั้นค่อยสร้าง admin UI | ⬜ |

## Track C — Platform / Ops

| รายการ | เหตุผล | สถานะ |
|---|---|---|
| CI: Flutter gate + DB gate (สอง workflow) | โค้ดและ SQL มีด่านเท่ากัน; DB suite รันทั้ง state ก่อน/หลัง publish | ✅ |
| Security posture: RLS deny-all, api-only exposure, function ACL hardening | ยืนยันแล้วด้วย 40+ assertion บนฐานจริง | ✅ |
| pg_cron: `ops.prune_api_access()` + `ops.assert_rights_invariants()` รายสัปดาห์ | access log ไม่โตไม่หยุด, smoke alarm สิทธิ์ทำงานเอง | ⬜ ตั้งใน dashboard |
| Nightly export ของ published rows → repo ส่วนตัว | สำรองแบบ diff ได้ + ทางหนี vendor lock-in; ห้ามรวม `editorial` | ⬜ |
| PITR | แผนฟรีมีแค่ daily snapshot — เมื่อเนื้อหาจริงเกินหลักร้อย รายการนี้คือประกันของทั้งบริษัท | ⬜ ต้อง Pro |
| Restore drill รายไตรมาส | backup ที่ไม่เคยลอง restore คือความเชื่อ ไม่ใช่ backup | ⬜ |

---

## หลักการที่ไม่เปลี่ยน (สรุปจาก RIGHTS-POLICY ฉบับเต็มใน Nimit-docs)

1. คำแปลทุกข้อความชี้กลับไปยัง passage ที่ตรวจสอบได้ — **ไม่มี citation = ไม่เผยแพร่**
2. ข้อความคำต่อคำเก็บได้เฉพาะงานที่พ้นลิขสิทธิ์/ได้รับอนุญาต — ฐานข้อมูลบังคับเอง ปิดไม่ได้
3. ป้าย tier มาจากบรรณานุกรมเสมอ (join จาก edition) — ป้ายกับที่มาขัดกันไม่ได้โดยโครงสร้าง
4. เนื้อหาจากแหล่งเดียวที่มีลิขสิทธิ์ ต้องมีแหล่งอิสระประกอบก่อนเผยแพร่ (กฎสองแหล่ง)
5. พุทธวรรณกรรมไม่ถูกแปลงเป็นเลขเสี่ยงโชค — มี assertion ใน test suite กันไว้ถาวร
6. เลขทุกตัวในแอปคือ "เลขเชิงสัญลักษณ์" ห้ามสื่อความแม่นหรือโอกาสถูกรางวัล
